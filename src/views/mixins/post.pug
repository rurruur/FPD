mixin post(info)
	div.post-block
		.post-title
			a(href=`/posts/${info.id}`)
				h4=info.title
			.post-commentCount
				i.far.fa-comment-dots
				| 
				small=info.commentCount
		if info.writer
				small=info.writer.nickname
		else 
			small 탈퇴한 사용자
		br
		small.createdAt=info.createdAt
		br
		small 조회수 #{info.views}